syntax = "proto3";

package data;

service ProtoService {
    rpc ProtoMethod (Request) returns (Response) {}
}

message Request {
    string request = 1;
}

message Response {
    string response = 1;
}